[user]
    email = totorovirus@gmail.com
    name = Jin Suk Park
[push]
    default = simple
[credential]
    helper = cache
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
